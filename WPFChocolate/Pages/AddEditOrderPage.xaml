<Page x:Class="WPFChocolate.Pages.AddEditOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFChocolate.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditOrderPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DatePicker Grid.Row="0" Name="date"/>
        <TextBox x:Name="tbClient" Grid.Row="1" Style="{StaticResource tbAuthSecond}"/>
        <Button x:Name="AddOrder" Content="Добавить заказ" Click="AddOrder_Click" Grid.Row="2" Width="150" Style="{StaticResource btnAuthtorize}"/>
        <Button x:Name="back" Content="Назад" Click="back_Click" Grid.Row="2" HorizontalAlignment="Left" Width="150" Style="{StaticResource btnAuthtorize}"/>
    </Grid>
</Page>
